const Index = ({ title }) => {
    return (
        <div className="py-12">
            <h1 className="text-3xl font-medium text-slate-500">{title}</h1>
        </div>
    );
};

export default Index;
